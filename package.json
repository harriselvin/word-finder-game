{
  "name": "word-finder-game",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p public && cp -r src/* public/",
    "deploy": "firebase deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "firebase": "^11.5.0"
  },
  "rules": {
    "leaderboard": {
      ".read": true,
      ".write": true
    }
  }
}
